# T-0023: Shape effects API (shadow, glow, reflection, soft edge)

**Status**: READY | **Size**: L
**Phase**: —

## Context
`ShadowFormat` exists but only exposes `.inherit` (boolean). No properties for shadow angle, blur, distance, color, or other effect types. An analysis document exists at `docs/dev/analysis/shp-shadow.rst` (343 lines) detailing the inheritance model and PowerPoint behaviors. Effects are preserved on round-trip via generic lxml handling.

## Acceptance Criteria
- [ ] `shape.shadow` returns expanded ShadowFormat with visible, type, angle, blur_radius, distance, color properties
- [ ] `shape.glow` returns GlowFormat with radius, color properties
- [ ] `shape.reflection` returns ReflectionFormat with basic properties
- [ ] `shape.soft_edge` returns SoftEdgeFormat with radius property
- [ ] OXML classes for effect list child elements
- [ ] Unit tests for each effect type
- [ ] BDD acceptance test for shadow formatting

## Verification Plan
(Filled by verification-planner agent during PLANNING)

### Hard Endpoints
Commands that must exit 0:
- [ ] `command` — what it proves

### Checkpoints
| Phase | Verification | Expected |
|-------|--------------|----------|
| TESTING_RED | Run new tests | FAIL |
| TESTING_GREEN | Run full suite | PASS |

### Evidence to Collect
- [ ] Output showing X

### Anti-Pattern Warnings
- Task-specific risks

## Approach
(Filled by implementation-approach-planner during PLANNING)

## Implementation Notes

## Obstacles

## Evidence

## Outcome
